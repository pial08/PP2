void main() {
  int a;
  bool done;

  a = 0;
  while (a < 10) {
	Print(a, " ");
	a = a + 1;
	if (a == 5) break;
  }
  if (a % 2 == 0)
     a = 1;
  else
     done = true;

  for (a = 1; a <= 10 || done;) {
     Print(a, " ");
     if (a == 10) a;
  }
  return;
}

